{
  "name": "football-academy-manager-pro",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Football Academy Manager Pro - React/Vite + Express/Prisma/PostgreSQL single-port app.",
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "npm --prefix frontend run dev --",
    "dev:frontend": "npm --prefix frontend run dev -- --host 0.0.0.0",
    "dev:backend": "npm --prefix backend run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "build:frontend": "npm --prefix frontend run build",
    "build:backend": "npm --prefix backend run build",
    "prisma:generate": "npm --prefix backend run prisma:generate",
    "db:push": "npm --prefix backend run db:push",
    "db:seed": "npm --prefix backend run db:seed",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down",
    "docker:reset": "docker compose down -v --remove-orphans && docker compose up --build"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
